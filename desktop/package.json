{
  "name": "holdfast-desktop",
  "version": "0.0.6",
  "private": true,
  "description": "Holdfast desktop client frontend (xterm.js tabs inside Tauri 2).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-unicode11": "^0.9.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.1.11"
  }
}
