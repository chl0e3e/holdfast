{
  "name": "holdfast-desktop",
  "version": "0.1.3",
  "private": true,
  "description": "Holdfast desktop client frontend (xterm.js tabs inside Tauri 2).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-msquic.mjs && tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "tsx src/ui-state.test.ts && tsx src/tab-label.test.ts && tsx src/terminal-render-state.test.ts && tsx src/terminal-layout.test.ts"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.0",
    "vite": "^7.1.11"
  }
}
