{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: app commands + core events only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
