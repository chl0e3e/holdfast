# Shared traversal stops at each target-account-owned 0700 upload directory.
# `holdfastd-multiuser.service` has PrivateTmp=no, so this is the same path seen
# by its socket-activated spawner and target shells.
d /tmp/holdfast-uploads 0711 holdfast holdfast 24h -
