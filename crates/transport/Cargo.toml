[package]
name = "hf-transport"
description = "Transport-neutral traits and channel model (QUIC, WebTransport, WebSocket fallback)."
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true
